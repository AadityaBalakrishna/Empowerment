in SpsCirrusContactMetadataRepository.java

	@Transactional(readOnly = true)
	@Query("SELECT * FROM SpsCirrusContactMetadataEntity m where m.metadataId in (:metadataId)")
	List<SpsCirrusContactMetadata> findExistingUsersByMetadataId(
            @Param("metadataId") UUID metadataId);

reviews:
review 1:
SELECT * is not valid JPQL.
SpsCirrusContactMetadataEntity is not your entity name; your entity is SpsCirrusContactMetadata.
IN (:metadataId) expects a collection, but you pass a single UUID.
Also , instead of this , use  a derived method : List findByMetadataIdIn(Collection metadataIds);
OR
@Query("select m from SpsCirrusContactMetadata m where m.metadataId in :metadataIds")
List findExistingUsersByMetadataId(@Param("metadataIds") Collection metadataIds);

