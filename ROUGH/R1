The performance log hasn’t been removed — it has been moved into the new adapter implementation. Since the old methods delegate to the adapter, the performance logging still occurs as before, ensuring we don’t lose latency visibility while avoiding duplication.
