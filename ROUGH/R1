in SpsCirrusContactMetadataRepository.java

	@Modifying
	@Transactional
	@Query("UPDATE sp_schema.sps_cirrus_contact_metadata SET retry_count = retry_count + 1  WHERE metadata_id = :metadataId")
	int incrementRetry(@Param("metadataId") UUID metadataId);

reviews:
when using JPL , table name should be entity name
@Query("update SpsCirrusContactMetadata m set m.retryCount = m.retryCount + 1 where m.metadataId = :metadataId")

