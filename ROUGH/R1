try to use only 1 time...
pseudo code
var className = ...getTarget().getClass().getSimpleName()
then use className for other refs.

for reusability & brevity u can wrap the coe in a method & use..e.g
getSimpleClassName (Event o)
{
return ...getTarget().getClass().getSimpleName()}
}
and use getSimpleClassName whr it is needed.

what should i do to work on these comments?
