	@Test
	void testGetByteBuffer() {
		String input = "test-string";
		ByteBuffer buffer = EpwKmsServiceImpl.getByteBuffer(input);
		assertNotNull(buffer);
		assertEquals(input, StandardCharsets.UTF_8.decode(buffer).toString());
	}

	@Test
	void testGetString() {
		String input = "test-string";
		ByteBuffer buffer = ByteBuffer.wrap(input.getBytes(StandardCharsets.UTF_8));
		String output = EpwKmsServiceImpl.getString(buffer);
		assertEquals(input, output);
	}

these two test classes are fro v1 impl, can i get the v2 impl?

	private ByteBuffer getByteBuffer(String string)
	{
		return ByteBuffer.wrap(string.getBytes(charset));
	}

	/**
	 * Converts a ByteBuffer to a String using the specified charset.
	 *
	 * @param byteBuffer
	 *            the ByteBuffer to convert
	 * @return the String representation of the ByteBuffer
	 */
	public static String getString(ByteBuffer byteBuffer)
	{
		byte[] byteArray = new byte[byteBuffer.remaining()];
		byteBuffer.get(byteArray);
		return new String(byteArray, charset);
	}

these are the v2 methods
