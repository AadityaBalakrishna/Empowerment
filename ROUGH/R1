catch (Exception e)
			{
				logger.info("Eviction on caching failure failed on {}, due to: {}, stacktrace: {}",
						pjp.toShortString(), ex.getMessage(), ExceptionUtils.getStackTrace(ex));

				Metrics.counter(CACHE_EVICTION_FAILURE_METRIC, "class", pjp.getTarget()
						.getClass()
						.getSimpleName(), "method",
						pjp.getSignature()
								.getName())
						.increment();
			}

let's go back to debugging this is how it is currently, how do i fix it?
