stage('Log Environment Variables') {
  steps {
    container("maven") {
      script {
        echo "HOST_IP: ${env.HOST_IP}"
        echo "DD_SERVICE: ${env.DD_SERVICE}"
        echo "DD_ENV: ${env.DD_ENV}"
        echo "DD_VERSION: ${env.DD_VERSION}"
      }
    }
  }
}
