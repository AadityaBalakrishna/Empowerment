String result = KMSServiceImpl.getInstance()
					.encryptUsingAWSKey(rawValue, this.getKeyManager()
							.getSystemPrimaryAliasName());

cryptengine previously had this, but getinstance doesnt exist anymore
