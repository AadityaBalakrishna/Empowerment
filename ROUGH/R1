package com.empower.epw.crm.gateway.web.controller;

import lombok.CustomLog;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@CustomLog
@RestController
@RequestMapping(value = "/api/gateway")
class SsmController
{
	@Value("${scaffold.test.secret}")
	private String scaffoldTestSecret;

	@Value("${crm.gateway.application}")
	private String crmGatewayApplication;

	@GetMapping(value = "/ssm")
	String transform()
	{
		log.info("Transform 'scaffold.test.secret' ssm value: " + scaffoldTestSecret);
		return scaffoldTestSecret;
	}

	@GetMapping(value = "/cfg")
	String getScaffoldTestKey()
	{
		log.info("Transform 'crm.gateway.application' cfg value: " + crmGatewayApplication);
		return crmGatewayApplication;
	}
}

and this is from the service_schema.yaml:
  /api/gateway/ssm:
    get:
      tags:
      - ssm-controller
      operationId: transform
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                type: string


say i change,  /api/gateway/ssm: to  /api/gateway/ssm-test: in the service schema, 

in SsmControllerApi.java, for transform() it becomes
--> return apiClient.invokeAPI("/api/gateway/ssm-test-town", HttpMethod.GET,

but this won't refer to the 
@GetMapping(value = "/ssm")
	String transform() in SsmController, 

Help me with a solution where, the SsmControllerApi doesn't get updated with the new api endpoint during the mvn clean install, it should throw an error during the build preventing the update
