✅ SafePageConfigurer is from a non-Spring project and is not a Spring-managed bean, so Spring cannot autowire it or use it within @SpringBootTest.
✅ Since it's not a bean, Spring does not execute its methods automatically, meaning it won't load properties unless explicitly called.
