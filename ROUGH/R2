1) for this methond i have received this comment - "this wont give a single value if multiple entries are there"

@Transactional(readOnly = true)
@Query("SELECT m.hashValue FROM SpsCirrusContactMetadata m WHERE m.userAccountGuid = :userAccountGuid ORDER BY m.updatedDate DESC, m.metadataId DESC")
Optional<Integer> findLatestHashByUserAccountGuid(@Param("userAccountGuid") UUID userAccountGuid);

 is there a test case which adds data to it and verifies in SpsCirrusContactMetadataRepositoryTest, if not, can you give me one that verifies it properly without leaving roam for doubt
